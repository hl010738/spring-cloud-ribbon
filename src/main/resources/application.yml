spring:
  application:
    name: eureka-ribbon-client
server:
  port: 8888

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    service-url:
      defaultZone: http://localhost:10097/eureka/
